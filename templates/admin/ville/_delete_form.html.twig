<form method="post" action="{{ path('admin_ville_delete', {'id': ville.id}) }}" onsubmit="return confirm('voulez vous vraiment supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ville.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
