{% extends 'admin/base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}
    <h1>Ville</h1>

    <table class="table " >
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ville.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ville.nom }}</td>
            </tr>
           
        </tbody>
    </table>
    <div class="d-flex">
    <a href="{{ path('admin_ville_index') }}" class="btn btn-primary pr-2">Retour a la liste</a>

    <a href="{{ path('admin_ville_edit', {'id': ville.id}) }}" class="btn btn-success mr-1">Modifier</a>

    {{ include('admin/ville/_delete_form.html.twig') }}
</div>

{% endblock %}
